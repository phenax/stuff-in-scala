package algos;

import org.scalatest._

class SortingAlgorithmsTest extends FlatSpec with Matchers {

	val bst= "The binary search tree";

	bst should "have only one node i.e. root" in {
		List() should have length 0
	}

	bst should "be cool" in {
		List() should have length 0
	}
}
